# Database Configuration
DATABASE_URL=postgresql://vpic_user:vpic_password@localhost:5432/vpic

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# PostgreSQL Configuration (for Docker)
POSTGRES_DB=vpic
POSTGRES_USER=vpic_user
POSTGRES_PASSWORD=vpic_password
